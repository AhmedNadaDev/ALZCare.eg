You are a Senior Frontend Architect (React + Tailwind CSS).

Before writing any code, you must FIRST create a complete UI/UX redesign plan for ONLY the Dashboard pages.

Context:
- This is an existing React project.
- Backend must NOT be touched.
- Only frontend dashboard UI will be improved.

Your task (PLANNING PHASE ONLY):

1. Analyze current dashboard structure (based on provided code)
2. Identify UI/UX problems
3. Propose a NEW modern dashboard layout (SaaS-level)
4. Suggest component restructuring (cards, grids, sliders, widgets, sidebar, header)
5. Decide what should become:
   - cards
   - charts
   - sliders/carousels
   - tables
   - quick actions sections
6. Improve information hierarchy (what appears first, second, etc.)
7. Make a clear section-by-section layout plan

Important rules:
- DO NOT write any code yet
- DO NOT implement anything
- ONLY produce a detailed architectural + UI/UX plan
- Focus only on dashboard pages
- Make it look like a modern production SaaS admin dashboard (very professional level)

Output format:
- Problems in current UI
- Proposed new layout structure
- Component breakdown
- UX improvements
- Responsive behavior strategy
- Final redesign concept summary